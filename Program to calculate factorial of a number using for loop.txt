#include <stdio.h>
int main()
{
    int i,n,fact=1;
    printf("Enter the number :");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
      printf("%d\n",i);
      fact=i;
    }
      printf("Factorial of the given no is %d",fact);
      return 0;
}